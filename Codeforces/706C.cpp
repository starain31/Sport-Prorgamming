#include "bits/stdc++.h"

#define DEBUG
#ifdef DEBUG
#define D(x) std::cout <<#x <<" := " <<x << std::endl
#else
#define D(x)
#endif

using namespace std;

typedef long long ll;
const int N = int(1e5+5);

int n;
string s;
string ps;
int c[N];

int main() {
	cin >>n;
	for(int i = 0; i < n; i++) {
		cin >>c[i];
	}
	cin >>ps;
	while(--n) {
		cin >>s;
		for(int i = 0; i < 

	return 0;
}
